interface:
  display_name: "TwinCAT Test"
  short_description: "Run fresh TwinCAT TcUnit verification"
  default_prompt: "Use $twincat-test to activate changed code or restart unchanged deployed code and collect fresh TcUnit evidence."
