interface:
  display_name: "TwinCAT Test"
  short_description: "Run linked TwinCAT and TcUnit tests"
  default_prompt: "Use $twincat-test to rebuild, activate the test profile, and collect TcUnit results."
